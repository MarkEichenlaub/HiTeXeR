diff --git a/_m8812.js b/_m8812.js
new file mode 100644
index 00000000..211bccef
--- /dev/null
+++ b/_m8812.js
@@ -0,0 +1,14 @@
+const sharp=require('sharp');
+(async()=>{
+  for(const [name,p] of [['TEXER','comparison/texer_pngs/08812.png'],['HTX','_08812.png']]){
+    const {data,info}=await sharp(p).flatten({background:{r:255,g:255,b:255}}).removeAlpha().raw().toBuffer({resolveWithObject:true});
+    const W=info.width,H=info.height,ch=info.channels;
+    const dark=(x,y)=>{const i=(y*W+x)*ch;return data[i]<140&&data[i+1]<140&&data[i+2]<140;};
+    // y-title: leftmost ~7% band, vertical extent of dark
+    const band=Math.floor(W*0.07);
+    let tT=H,tB=0;for(let y=0;y<H;y++)for(let x=0;x<band;x++)if(dark(x,y)){if(y<tT)tT=y;if(y>tB)tB=y;break;}
+    // plot grid: the gray plot area — find topmost & bottommost dark in the middle band (x 30%-70%)
+    let gT=H,gB=0;for(let y=0;y<H;y++){let c=0;for(let x=Math.floor(W*0.3);x<Math.floor(W*0.7);x++)if(dark(x,y))c++;if(c>3){if(y<gT)gT=y;if(y>gB)gB=y;}}
+    console.log(name,'H='+H,'y-title y=['+tT+','+tB+'] h='+(tB-tT),'plot(mid) y=['+gT+','+gB+'] h='+(gB-gT),'titleTopVsPlotTop='+(tT-gT));
+  }
+})();
diff --git a/_meas4452.js b/_meas4452.js
new file mode 100644
index 00000000..8f644b8d
--- /dev/null
+++ b/_meas4452.js
@@ -0,0 +1,15 @@
+const sharp=require('sharp');
+(async()=>{
+  for(const [name,p] of [['TEXER','comparison/texer_pngs/04452.png'],['HTX','_04452.png']]){
+    const {data,info}=await sharp(p).flatten({background:{r:255,g:255,b:255}}).removeAlpha().raw().toBuffer({resolveWithObject:true});
+    const W=info.width,H=info.height,ch=info.channels;
+    const isCyan=(r,g,b)=>r<150&&g>120&&b>120&&Math.abs(g-b)<60&&g>r+30;
+    const isBlack=(r,g,b)=>r<90&&g<90&&b<90;
+    // grid cyan vertical extent
+    let gTop=H,gBot=0;for(let y=0;y<H;y++)for(let x=0;x<W;x++){const i=(y*W+x)*ch;if(isCyan(data[i],data[i+1],data[i+2])){if(y<gTop)gTop=y;if(y>gBot)gBot=y;break;}}
+    // legend box: black horizontal lines in the lower part (below grid). find rows with long black runs below gBot
+    let legTop=0,legBot=0;
+    for(let y=gBot+2;y<H;y++){let c=0;for(let x=0;x<W;x++){const i=(y*W+x)*ch;if(isBlack(data[i],data[i+1],data[i+2]))c++;}if(c>W*0.2){if(!legTop)legTop=y;legBot=y;}}
+    console.log(name,'H='+H,'grid y=['+gTop+','+gBot+'] gridH='+(gBot-gTop),'legendBox y=['+legTop+','+legBot+']','gap(grid->legend)='+(legTop?legTop-gBot:'n/a'));
+  }
+})();
diff --git a/_zbump.js b/_zbump.js
new file mode 100644
index 00000000..ec407fa4
--- /dev/null
+++ b/_zbump.js
@@ -0,0 +1,12 @@
+const sharp=require('sharp');
+(async()=>{
+  for(const [name,p] of [['tex','comparison/texer_pngs/03290.png'],['htx','_03290.png']]){
+    const m=await sharp(p).metadata();
+    // top half (the bump), full width
+    await sharp(p).extract({left:0,top:0,width:m.width,height:Math.floor(m.height*0.45)}).resize({width:700}).flatten({background:'#fff'}).png().toFile('_zb_'+name+'.png');
+  }
+  const a=await sharp('_zb_tex.png').metadata(),b=await sharp('_zb_htx.png').metadata();
+  await sharp({create:{width:Math.max(a.width,b.width),height:a.height+8+b.height,channels:4,background:'#999999ff'}})
+    .composite([{input:'_zb_tex.png',left:0,top:0},{input:'_zb_htx.png',left:0,top:a.height+8}]).png().toFile('_zb290.png');
+  console.log('done (top=TEXER, bottom=HiTeXeR)');
+})();
diff --git a/auto-fix/enqueue-history.jsonl b/auto-fix/enqueue-history.jsonl
index 0b37e447..a09c80a5 100644
--- a/auto-fix/enqueue-history.jsonl
+++ b/auto-fix/enqueue-history.jsonl
@@ -658,3 +658,12 @@
 {"enqueueId":"mqi0xcoyobji","id":"05845","description":"","enqueuedAt":"2026-06-17T12:04:58.114Z"}
 {"enqueueId":"mqi1i74vs4xe","id":"04531","description":"too big","enqueuedAt":"2026-06-17T12:21:10.687Z"}
 {"enqueueId":"mqi2mcljvsy0","id":"03290","description":"the surface should not be transparent","enqueuedAt":"2026-06-17T12:52:24.007Z"}
+{"enqueueId":"mqkvontks3jw","id":"12980","description":"missing head of mallet","enqueuedAt":"2026-06-19T12:01:33.080Z"}
+{"enqueueId":"mqkxfovhpucn","id":"05050","description":"literal } in title","enqueuedAt":"2026-06-19T12:50:33.773Z"}
+{"enqueueId":"mqkyq94641qb","id":"05553","description":"The word \"meters\" should be in upright font, but it is italicized.","enqueuedAt":"2026-06-19T13:26:46.182Z"}
+{"enqueueId":"mql0ebygp16y","id":"00600","description":"Segments AB, BC, CE, and EA are all missing.","enqueuedAt":"2026-06-19T14:13:29.225Z"}
+{"enqueueId":"mql1vseybw0s","id":"04156","description":"axes start and stop in wrong places","enqueuedAt":"2026-06-19T14:55:03.322Z"}
+{"enqueueId":"mql32i5ir8n2","id":"04452","description":"legend box cuts through legend, entire thing too small","enqueuedAt":"2026-06-19T15:28:16.230Z"}
+{"enqueueId":"mql68jebth7d","id":"04702","description":"linebreaks in wrong places","enqueuedAt":"2026-06-19T16:56:56.627Z"}
+{"enqueueId":"mql9twj455lo","id":"08743","description":"aspect ratio way off, missing tick mars on both axes","enqueuedAt":"2026-06-19T18:37:32.272Z"}
+{"enqueueId":"mqlc3lz9y86n","id":"04943","description":"underbrace goes too far, 2 is not centered ","enqueuedAt":"2026-06-19T19:41:04.389Z"}
diff --git a/auto-fix/fix-snapshots/0337620f-after.png b/auto-fix/fix-snapshots/0337620f-after.png
new file mode 100644
index 00000000..2989d46d
Binary files /dev/null and b/auto-fix/fix-snapshots/0337620f-after.png differ
diff --git a/auto-fix/fix-snapshots/73439c17-after.png b/auto-fix/fix-snapshots/73439c17-after.png
new file mode 100644
index 00000000..b54b8563
Binary files /dev/null and b/auto-fix/fix-snapshots/73439c17-after.png differ
diff --git a/auto-fix/fix-snapshots/ef2325ce-after.png b/auto-fix/fix-snapshots/ef2325ce-after.png
new file mode 100644
index 00000000..b54b8563
Binary files /dev/null and b/auto-fix/fix-snapshots/ef2325ce-after.png differ
diff --git a/auto-fix/queue.json b/auto-fix/queue.json
index 2d247f4a..a1b51a8a 100644
--- a/auto-fix/queue.json
+++ b/auto-fix/queue.json
@@ -1,54 +1,4 @@
 [
-  {
-    "id": "12980",
-    "description": "missing head of mallet",
-    "addedAt": "2026-06-14T20:42:40.454Z"
-  },
-  {
-    "id": "05050",
-    "description": "literal } in title",
-    "addedAt": "2026-06-15T07:07:04.328Z"
-  },
-  {
-    "id": "05553",
-    "description": "The word \"meters\" should be in upright font, but it is italicized.",
-    "addedAt": "2026-06-15T12:16:42.218Z"
-  },
-  {
-    "id": "00600",
-    "description": "Segments AB, BC, CE, and EA are all missing.",
-    "addedAt": "2026-06-15T12:21:46.571Z"
-  },
-  {
-    "id": "04156",
-    "description": "axes start and stop in wrong places",
-    "addedAt": "2026-06-17T06:45:53.940Z"
-  },
-  {
-    "id": "04452",
-    "description": "legend box cuts through legend, entire thing too small",
-    "addedAt": "2026-06-17T06:47:52.969Z"
-  },
-  {
-    "id": "04702",
-    "description": "linebreaks in wrong places",
-    "addedAt": "2026-06-17T06:48:18.533Z"
-  },
-  {
-    "id": "05051",
-    "description": "literal { and } show up in the labels",
-    "addedAt": "2026-06-17T06:49:10.282Z"
-  },
-  {
-    "id": "08743",
-    "description": "aspect ratio way off, missing tick mars on both axes",
-    "addedAt": "2026-06-17T06:49:46.795Z"
-  },
-  {
-    "id": "04943",
-    "description": "underbrace goes too far, 2 is not centered ",
-    "addedAt": "2026-06-17T06:50:42.866Z"
-  },
   {
     "id": "03281",
     "description": "unfill, grid on the solid doesn't track its surface, shading colors wrong",
diff --git a/auto-fix/rejected-edits/mqkxey5f7i4s.patch b/auto-fix/rejected-edits/mqkxey5f7i4s.patch
new file mode 100644
index 00000000..14b5ed8c
--- /dev/null
+++ b/auto-fix/rejected-edits/mqkxey5f7i4s.patch
@@ -0,0 +1,141 @@
+diff --git a/asy-interp.js b/asy-interp.js
+index 80e54880..254a0444 100644
+--- a/asy-interp.js
++++ b/asy-interp.js
+@@ -10167,6 +10167,56 @@ function createInterpreter() {
+     env.set('filldraw', (...args) => evalDraw('filldraw', args));
+     env.set('clip', (...args) => evalDraw('clip', args));
+     env.set('unfill', (...args) => evalDraw('unfill', args));
++    // axialshade(picture pic=currentpicture, path[] g, bool stroke=false,
++    //            pen pena, pair a, pen penb, pair b):
++    // Fill path g with an axial (linear) gradient running from color pena at
++    // world point a to color penb at world point b. Asymptote's PostScript
++    // `axial` shading pads to pena before a and penb after b. We emit one
++    // `fill` command carrying _axialGradient; the SVG emitter converts the two
++    // world endpoints to pixel space and emits a <linearGradient> with two
++    // stops (default spreadMethod="pad" matches the PS pad semantics).
++    // Common AoPS idiom: axialshade(circle(c,r), black,(x0,y0), palegray,(x1,y1))
++    // for a shaded sphere/ball (e.g. 12980's mallet head). Endpoints a/b are
++    // taken in the path's own (world) coordinate space, so this matches when g
++    // is an untransformed path; a shift()*path with literal a/b would not.
++    env.set('axialshade', (...args) => {
++      const pos = args.filter(a => !(a && a._named));
++      let target = currentPic;
++      let pathArg = null;
++      const pens = [];
++      const pairs = [];
++      for (const a of pos) {
++        if (a && a._tag === 'picture' && target === currentPic) { target = a; continue; }
++        if (isPath(a) && !pathArg) { pathArg = a; continue; }
++        if (Array.isArray(a) && a.length > 0 && isPath(a[0]) && !pathArg) { pathArg = a[0]; continue; }
++        if (isPen(a)) { pens.push(a); continue; }
++        if (isPair(a)) { pairs.push(a); continue; }
++      }
++      for (const a of args) {
++        if (a && a._named) {
++          if ('pic' in a && a.pic && a.pic._tag === 'picture') target = a.pic;
++          if ('g' in a && isPath(a.g)) pathArg = a.g;
++          if ('pena' in a && isPen(a.pena)) pens[0] = a.pena;
++          if ('penb' in a && isPen(a.penb)) pens[1] = a.penb;
++          if ('a' in a && isPair(a.a)) pairs[0] = a.a;
++          if ('b' in a && isPair(a.b)) pairs[1] = a.b;
++        }
++      }
++      if (!pathArg || pens.length < 2 || pairs.length < 2) return;
++      const ca = {r: pens[0].r || 0, g: pens[0].g || 0, b: pens[0].b || 0};
++      const cb = {r: pens[1].r || 0, g: pens[1].g || 0, b: pens[1].b || 0};
++      const line = args._line || 0;
++      const fillPen = clonePen(pens[0]);
++      fillPen.opacity = 1;
++      target.commands.push({
++        cmd: 'fill', path: pathArg, pen: fillPen, line,
++        _axialGradient: {
++          ax: pairs[0].x, ay: pairs[0].y,
++          bx: pairs[1].x, by: pairs[1].y,
++          ca, cb,
++        },
++      });
++    });
+     env.set('label', (...args) => evalLabel(args));
+ 
+     // labelpath(string s, path g[, pen p, ...]) — from labelpath.asy. Lay text
+@@ -30053,6 +30103,32 @@ function renderSVG(result, opts) {
+         );
+         fill = `url(#${gid})`;
+       }
++      // Axial (linear) gradient fill from axialshade(). World endpoints in
++      // dc._axialGradient.{ax,ay}->{bx,by} convert to SVG pixel coords with the
++      // same transform as paths; emit an inline <linearGradient> (spreadMethod
++      // defaults to "pad", matching Asymptote's PS `axial` pad semantics).
++      if (dc._axialGradient) {
++        const ag = dc._axialGradient;
++        const ax = (ag.ax - minX) * pxPerUnitX;
++        const ay = (maxY - ag.ay) * pxPerUnitY;
++        const bx = (ag.bx - minX) * pxPerUnitX;
++        const by = (maxY - ag.by) * pxPerUnitY;
++        const _rgb = c => `rgb(${Math.round(c.r*255)},${Math.round(c.g*255)},${Math.round(c.b*255)})`;
++        if (typeof window !== 'undefined') {
++          window._axialGradCounter = (window._axialGradCounter || 0) + 1;
++        } else {
++          if (typeof globalThis._axialGradCounter !== 'number') globalThis._axialGradCounter = 0;
++          globalThis._axialGradCounter += 1;
++        }
++        const gid = `_ag${(typeof window !== 'undefined' ? window._axialGradCounter : globalThis._axialGradCounter)}`;
++        elements.push(
++          `<defs><linearGradient id="${gid}" x1="${fmt(ax)}" y1="${fmt(ay)}" x2="${fmt(bx)}" y2="${fmt(by)}" gradientUnits="userSpaceOnUse">` +
++          `<stop offset="0%" stop-color="${_rgb(ag.ca)}"/>` +
++          `<stop offset="100%" stop-color="${_rgb(ag.cb)}"/>` +
++          `</linearGradient></defs>`
++        );
++        fill = `url(#${gid})`;
++      }
+     } else if (dc.cmd === 'filldraw') {
+       // If fill pen is invisible (opacity 0), use fill='none' so the stroke outline remains visible
+       fill = (css.opacity === 0) ? 'none' : css.fill;
+diff --git a/auto-fix/fix-snapshots/18568fd6-after.png b/auto-fix/fix-snapshots/18568fd6-after.png
+new file mode 100644
+index 00000000..efa68640
+Binary files /dev/null and b/auto-fix/fix-snapshots/18568fd6-after.png differ
+diff --git a/auto-fix/fix-snapshots/cb16d21e-after.png b/auto-fix/fix-snapshots/cb16d21e-after.png
+new file mode 100644
+index 00000000..efa68640
+Binary files /dev/null and b/auto-fix/fix-snapshots/cb16d21e-after.png differ
+diff --git a/auto-fix/fix-snapshots/df8c1724-after.png b/auto-fix/fix-snapshots/df8c1724-after.png
+new file mode 100644
+index 00000000..efa68640
+Binary files /dev/null and b/auto-fix/fix-snapshots/df8c1724-after.png differ
+diff --git a/comparison/ssim-results.json b/comparison/ssim-results.json
+index e5e2322e..d7807614 100644
+--- a/comparison/ssim-results.json
++++ b/comparison/ssim-results.json
+@@ -581,7 +581,7 @@
+     "id": "12857",
+     "idx": 12856,
+     "corpusFile": "gallery_axialshade.asy",
+-    "ssim": 0.3935561256811923,
++    "ssim": 0.9811544159257064,
+     "rawSsim": 0.3918407187887074,
+     "softSsim": 0.3935561256811923,
+     "sizeScore": 0.000001324592864586669,
+@@ -62821,7 +62821,7 @@
+     "id": "12980",
+     "idx": 12979,
+     "corpusFile": "phys_lib_sound_wave_in_box.asy",
+-    "ssim": 0.9806309430186769,
++    "ssim": 0.9908158218281242,
+     "rawSsim": 0.9198480777267611,
+     "softSsim": 0.9806309430186769,
+     "sizeScore": 0.9996110379695443,
+diff --git a/index.html b/index.html
+index 4e67e8a0..984a4be4 100644
+--- a/index.html
++++ b/index.html
+@@ -656,7 +656,7 @@ body {
+ <body>
+ 
+ <div id="toolbar">
+-  <h1>HiTeXeR <span style="font-size:0.675em;font-weight:normal;opacity:0.6">v9.11</span></h1>
++  <h1>HiTeXeR <span style="font-size:0.675em;font-weight:normal;opacity:0.6">v9.12</span></h1>
+   <div class="menu-bar">
+     <div class="menu-item" id="menu-file">
+       <span>File</span>
diff --git a/auto-fix/rejected-edits/mqkypj5nphul.patch b/auto-fix/rejected-edits/mqkypj5nphul.patch
new file mode 100644
index 00000000..58c2f43e
--- /dev/null
+++ b/auto-fix/rejected-edits/mqkypj5nphul.patch
@@ -0,0 +1,78 @@
+diff --git a/asy-interp.js b/asy-interp.js
+index 80e54880..f1a634ad 100644
+--- a/asy-interp.js
++++ b/asy-interp.js
+@@ -31338,6 +31338,26 @@ function renderSVG(result, opts) {
+               displayText = '$' + innerStripped + '$';
+             }
+           }
++        } else if (trimmed2.startsWith('{') && trimmed2.endsWith('}')) {
++          // Non-math text that was wrapped in the size command's grouping braces,
++          // e.g. "{\Large Anatomy of a Parabola}" → "Anatomy of a Parabola". The
++          // outer {...} is a TeX group scoping \Large; once the selector is
++          // stripped the braces are pure grouping and must NOT render as literal
++          // glyphs (this label routes to the plain-text SVG path, where { } draw).
++          // Only strip a single outer group that spans the whole string (depth
++          // returns to 0 solely at the final brace), so command-argument braces
++          // (the {Title} in \textbf{Title}) and multi-group labels ("{A} and
++          // {B}") are left intact. Escaped \{ \} (set-builder math) don't count.
++          let depth = 0, single = true;
++          for (let i = 0; i < trimmed2.length; i++) {
++            const c = trimmed2[i];
++            if (c === '{' && trimmed2[i - 1] !== '\\') depth++;
++            else if (c === '}' && trimmed2[i - 1] !== '\\') {
++              depth--;
++              if (depth === 0 && i !== trimmed2.length - 1) { single = false; break; }
++            }
++          }
++          if (single && depth === 0) displayText = trimmed2.slice(1, -1).trim();
+         }
+         // Apply LaTeX font-size selectors as absolute (capped at 12pt baseline)
+         // — see longer comment above the matching pre-scale block. Without this,
+diff --git a/auto-fix/fix-snapshots/501c7051-after.png b/auto-fix/fix-snapshots/501c7051-after.png
+new file mode 100644
+index 00000000..3ee4d99a
+Binary files /dev/null and b/auto-fix/fix-snapshots/501c7051-after.png differ
+diff --git a/auto-fix/fix-snapshots/78bff23e-after.png b/auto-fix/fix-snapshots/78bff23e-after.png
+new file mode 100644
+index 00000000..3ee4d99a
+Binary files /dev/null and b/auto-fix/fix-snapshots/78bff23e-after.png differ
+diff --git a/auto-fix/fix-snapshots/bdf27bbf-after.png b/auto-fix/fix-snapshots/bdf27bbf-after.png
+new file mode 100644
+index 00000000..3ee4d99a
+Binary files /dev/null and b/auto-fix/fix-snapshots/bdf27bbf-after.png differ
+diff --git a/comparison/ssim-results.json b/comparison/ssim-results.json
+index e5e2322e..fa45447c 100644
+--- a/comparison/ssim-results.json
++++ b/comparison/ssim-results.json
+@@ -14321,7 +14321,7 @@
+     "id": "05051",
+     "idx": 5050,
+     "corpusFile": "c398_L6_script_4.asy",
+-    "ssim": 0.9761289937249201,
++    "ssim": 0.9886958975203196,
+     "rawSsim": 0.6114138305446855,
+     "softSsim": 0.9761289937249201,
+     "sizeScore": 0.953164541048494,
+@@ -18121,7 +18121,7 @@
+     "id": "05050",
+     "idx": 5049,
+     "corpusFile": "c398_L6_script_3.asy",
+-    "ssim": 0.9812447734983659,
++    "ssim": 0.989082548041133,
+     "rawSsim": 0.7296863681774121,
+     "softSsim": 0.9812447734983659,
+     "sizeScore": 0.9618581700032282,
+diff --git a/index.html b/index.html
+index 4e67e8a0..984a4be4 100644
+--- a/index.html
++++ b/index.html
+@@ -656,7 +656,7 @@ body {
+ <body>
+ 
+ <div id="toolbar">
+-  <h1>HiTeXeR <span style="font-size:0.675em;font-weight:normal;opacity:0.6">v9.11</span></h1>
++  <h1>HiTeXeR <span style="font-size:0.675em;font-weight:normal;opacity:0.6">v9.12</span></h1>
+   <div class="menu-bar">
+     <div class="menu-item" id="menu-file">
+       <span>File</span>
diff --git a/auto-fix/rejected-edits/mql0d46xdm75.patch b/auto-fix/rejected-edits/mql0d46xdm75.patch
new file mode 100644
index 00000000..072402e5
--- /dev/null
+++ b/auto-fix/rejected-edits/mql0d46xdm75.patch
@@ -0,0 +1,113 @@
+diff --git a/asy-interp.js b/asy-interp.js
+index 80e54880..a959aa85 100644
+--- a/asy-interp.js
++++ b/asy-interp.js
+@@ -26881,7 +26881,14 @@ function renderSVG(result, opts) {
+         if (dc.align) {
+           const ax = dc.align.x, ay = dc.align.y;
+           const marginX = 0.40 * fontSize / roughPxPerUnitX;
+-          const marginY = 0.40 * fontSize / roughPxPerUnitY;
++          // Vertical margin matches the render's labelmargin (0.28*fontsize, the
++          // real Asymptote plain_pens.asy value used at the draw site ~30972).
++          // Horizontal keeps the 0.40 safety because the char-width heuristic
++          // under-estimates glyph advances; vertical height is measured (hBp),
++          // so the extra 0.12 was pure over-reservation — and for high-magnitude
++          // aligns like 3*S it is tripled, padding ~6.5bp of empty space below
++          // an S-anchored caption (05553's "5 meters" canvas ran ~27px too tall).
++          const marginY = 0.28 * fontSize / roughPxPerUnitY;
+           // Match Asymptote drawlabel.cc: Align *= 0.5 / max(|ax|,|ay|).
+           // The text-box offset is L-infinity normalised; magnitude only scales labelmargin.
+           const aInfMax = Math.max(Math.abs(ax), Math.abs(ay));
+@@ -26990,7 +26997,8 @@ function renderSVG(result, opts) {
+             const _axNAl = _aInfMaxAl > 0 ? (_axAl * 0.5 / _aInfMaxAl) : 0;
+             const _ayNAl = _aInfMaxAl > 0 ? (_ayAl * 0.5 / _aInfMaxAl) : 0;
+             let alignOffsetXBp = dc.align ? (_axNAl * lWidthBp + _axAl * 0.40 * fontSize) : 0;
+-            let alignOffsetYBp = dc.align ? (_ayNAl * lHeightBp + _ayAl * 0.40 * fontSize) : 0;
++            // Vertical margin = render labelmargin (0.28); see marginY note above.
++            let alignOffsetYBp = dc.align ? (_ayNAl * lHeightBp + _ayAl * 0.28 * fontSize) : 0;
+             // screenDx/screenDy (in bp) are applied on top of alignment-driven offset
+             if (dc.screenDx) alignOffsetXBp += dc.screenDx;
+             if (dc.screenDy) alignOffsetYBp -= dc.screenDy;  // SVG y-down → Asymptote y-up
+@@ -31478,6 +31486,26 @@ function renderSVG(result, opts) {
+       const hasLaTeX = !usesInlineColor && /\\(frac|underbrace|overbrace|sqrt)\b/.test(displayText);
+       const hasMath = /\$/.test(displayText) || /\\[a-zA-Z]/.test(displayText);
+ 
++      // Asymptote label strings with no $...$ are LaTeX LR (text) mode: upright
++      // glyphs, inter-word spaces preserved. A bare \color{...}/\textcolor wrapper
++      // does NOT switch to math mode — but \color matched \\[a-zA-Z] above, so
++      // hasMath is true and the label would route through KaTeX *math* mode
++      // (italic letters, collapsed spaces — e.g. "{\color{blue}5 meters}" in
++      // 05553 rendered "5meters" italic instead of upright "5 meters").
++      // Detect "text with color": no $, and once the color commands and grouping
++      // braces are removed, nothing but ordinary text/digits/punctuation remains
++      // (no other LaTeX command, no ^/_ scripts). Render those in text mode with
++      // the inline color preserved.
++      let isTextWithColor = false;
++      if (!/\$/.test(displayText) && /\\(?:color|textcolor)\b/.test(displayText)) {
++        const txtProbe = displayText
++          .replace(/\\textcolor\s*\{[^}]*\}/g, '')
++          .replace(/\\color\s*\[[^\]]*\]\s*\{[^}]*\}/g, '')
++          .replace(/\\color\s*\{[^}]*\}/g, '')
++          .replace(/[{}]/g, '');
++        if (!/\\[a-zA-Z]/.test(txtProbe) && !/[\^_]/.test(txtProbe)) isTextWithColor = true;
++      }
++
+       // Check if math content uses only LaTeX commands with known Unicode equivalents
+       // (Greek letters, operators, etc.) plus simple ^/_ scripts.  Such labels render
+       // more reliably as SVG <text> (scales with the viewBox) than as KaTeX foreignObject
+@@ -31583,6 +31611,17 @@ function renderSVG(result, opts) {
+         if (/\\(?:bf|bfseries|mathbf|textbf|boldsymbol)(?![a-zA-Z])/.test(displayText)) {
+           labelEl = `<g font-weight="bold" stroke="${css.fill}" stroke-width="${fmt(effectiveFontSize*0.04)}">${labelEl}</g>`;
+         }
++      } else if (isTextWithColor && (typeof katex !== 'undefined' || svgNativeMode)) {
++        // Text-mode label carrying an inline \color/\textcolor (no $...$): wrap
++        // the whole string in \text{} so KaTeX renders upright glyphs with the
++        // inter-word space preserved, while the color command still applies
++        // inside text mode (verified: \text{{\color{blue}5 meters}} → upright,
++        // spaced, blue). Mirrors the math-mode KaTeX routing just below.
++        const _txtInput = '\\text{' + displayText + '}';
++        labelEl = renderLabelKatexSvg(_txtInput, fmt(sx+dx), fmt(sy+dy), effectiveFontSize, css.fill, anchor, baseline, css.opacity, effectiveFontSizeCSS, _labelHEst, _labelAyN)
++          || ((opts && opts.labelOutput === 'svg-native')
++                ? renderLabelMathJaxSVG(_txtInput, fmt(sx+dx), fmt(sy+dy), effectiveFontSize, css.fill, anchor, baseline, css.opacity, effectiveFontSizeCSS, _labelHEst, _labelAyN)
++                : renderLabelKaTeX(_txtInput, fmt(sx+dx), fmt(sy+dy), effectiveFontSize, css.fill, anchor, baseline, css.opacity, effectiveFontSizeCSS));
+       } else if ((typeof katex !== 'undefined' || svgNativeMode) && hasMath && !unicodeSafe) {
+         // NOTE: the `|| svgNativeMode` matters — in node `katex` is undefined, so
+         // without it every mixed text+math label ("$W-1$ cells"), accent command
+diff --git a/auto-fix/fix-snapshots/5d95fe2c-after.png b/auto-fix/fix-snapshots/5d95fe2c-after.png
+new file mode 100644
+index 00000000..fc32ee4f
+Binary files /dev/null and b/auto-fix/fix-snapshots/5d95fe2c-after.png differ
+diff --git a/auto-fix/fix-snapshots/b8a33c09-after.png b/auto-fix/fix-snapshots/b8a33c09-after.png
+new file mode 100644
+index 00000000..fc32ee4f
+Binary files /dev/null and b/auto-fix/fix-snapshots/b8a33c09-after.png differ
+diff --git a/auto-fix/fix-snapshots/e5d4a347-after.png b/auto-fix/fix-snapshots/e5d4a347-after.png
+new file mode 100644
+index 00000000..fc32ee4f
+Binary files /dev/null and b/auto-fix/fix-snapshots/e5d4a347-after.png differ
+diff --git a/comparison/ssim-results.json b/comparison/ssim-results.json
+index e5e2322e..c0f44c76 100644
+--- a/comparison/ssim-results.json
++++ b/comparison/ssim-results.json
+@@ -8161,7 +8161,7 @@
+     "id": "05553",
+     "idx": 5552,
+     "corpusFile": "c401_L3_script_1.asy",
+-    "ssim": 0.956985496561331,
++    "ssim": 0.9746918136404219,
+     "rawSsim": 0.9216835693329349,
+     "softSsim": 0.956985496561331,
+     "sizeScore": 0.9110450973963355,
+diff --git a/index.html b/index.html
+index 4e67e8a0..984a4be4 100644
+--- a/index.html
++++ b/index.html
+@@ -656,7 +656,7 @@ body {
+ <body>
+ 
+ <div id="toolbar">
+-  <h1>HiTeXeR <span style="font-size:0.675em;font-weight:normal;opacity:0.6">v9.11</span></h1>
++  <h1>HiTeXeR <span style="font-size:0.675em;font-weight:normal;opacity:0.6">v9.12</span></h1>
+   <div class="menu-bar">
+     <div class="menu-item" id="menu-file">
+       <span>File</span>
diff --git a/auto-fix/rejected-edits/mql1uvtej8fm.patch b/auto-fix/rejected-edits/mql1uvtej8fm.patch
new file mode 100644
index 00000000..36120352
--- /dev/null
+++ b/auto-fix/rejected-edits/mql1uvtej8fm.patch
@@ -0,0 +1,87 @@
+diff --git a/asy-interp.js b/asy-interp.js
+index 80e54880..09fa08f1 100644
+--- a/asy-interp.js
++++ b/asy-interp.js
+@@ -10839,6 +10839,35 @@ function createInterpreter() {
+     // D() — cse5/olympiad shorthand for draw that returns its path
+     // In AoPS/cse5 convention, D() uses blue pen by default (unlike draw() which uses black)
+     env.set('D', (...args) => {
++      // cse5 marked-point form: D(Label L, pair A, pen fontpen, pair align).
++      // This draws a DOT at A (with `pointpen`), places the label L at A using
++      // the optional font pen + alignment, and RETURNS the pair A so the point
++      // chains inside a path expression. AoPS uses it to label+dot+connect every
++      // vertex in one expression, e.g.
++      //   draw(D("A",A,s,dir(90))--D("B",B,s,SW)--A--D("E",E,s,S)--C--...);
++      // Without this branch the call (a string + two pairs, no path) fell
++      // through to the path-draw form, which found no path and returned an
++      // EMPTY path — collapsing the whole chain to the few segments between the
++      // bare points and dropping every vertex dot (00600 drew only A--C).
++      {
++        let hasStrL = false, hasPathL = false, firstPairL = null;
++        for (const a of args) {
++          if (isString(a)) hasStrL = true;
++          else if (isPath(a)) hasPathL = true;
++          else if ((isPair(a) || isTriple(a) || isPoint(a)) && !firstPairL) firstPairL = a;
++        }
++        if (hasStrL && firstPairL && !hasPathL) {
++          // Place the label via MP (handles $...$ wrapping, align defaults).
++          const mp = env.get('MP');
++          const placed = (typeof mp === 'function') ? mp(...args) : firstPairL;
++          // Draw the vertex dot with pointpen (cse5's D draws the dot itself).
++          const ptp = env.get('pointpen');
++          const dotArgs = [firstPairL, (isPen(ptp) ? ptp : clonePen(defaultPen))];
++          dotArgs._line = args._line;
++          evalDot(dotArgs);
++          return placed || firstPairL;
++        }
++      }
+       // cse5 overloads D(): a lone pair/point draws a DOT with `pointpen`
+       //     D(pair A, pen p=pointpen) { dot(A,p); }
+       // while a path draws with `pathpen`
+diff --git a/auto-fix/fix-snapshots/35fb462e-after.png b/auto-fix/fix-snapshots/35fb462e-after.png
+new file mode 100644
+index 00000000..1c9b3738
+Binary files /dev/null and b/auto-fix/fix-snapshots/35fb462e-after.png differ
+diff --git a/auto-fix/fix-snapshots/72dbe1ca-after.png b/auto-fix/fix-snapshots/72dbe1ca-after.png
+new file mode 100644
+index 00000000..1c9b3738
+Binary files /dev/null and b/auto-fix/fix-snapshots/72dbe1ca-after.png differ
+diff --git a/auto-fix/fix-snapshots/7d77f2e7-after.png b/auto-fix/fix-snapshots/7d77f2e7-after.png
+new file mode 100644
+index 00000000..1c9b3738
+Binary files /dev/null and b/auto-fix/fix-snapshots/7d77f2e7-after.png differ
+diff --git a/comparison/ssim-results.json b/comparison/ssim-results.json
+index e5e2322e..c0d557a4 100644
+--- a/comparison/ssim-results.json
++++ b/comparison/ssim-results.json
+@@ -24761,7 +24761,7 @@
+     "id": "00600",
+     "idx": 599,
+     "corpusFile": "c134_L1_script_2.asy",
+-    "ssim": 0.9591827377373613,
++    "ssim": 0.9826726351386175,
+     "rawSsim": 0.798029132870339,
+     "softSsim": 0.9591827377373613,
+     "sizeScore": 0.9992082539939208,
+@@ -33541,7 +33541,7 @@
+     "id": "00607",
+     "idx": 606,
+     "corpusFile": "c134_L1_script_3.asy",
+-    "ssim": 0.9677257474643344,
++    "ssim": 0.9910597286551737,
+     "rawSsim": 0.7800945610166721,
+     "softSsim": 0.9677257474643344,
+     "sizeScore": 0.9997145585167292,
+diff --git a/index.html b/index.html
+index 4e67e8a0..984a4be4 100644
+--- a/index.html
++++ b/index.html
+@@ -656,7 +656,7 @@ body {
+ <body>
+ 
+ <div id="toolbar">
+-  <h1>HiTeXeR <span style="font-size:0.675em;font-weight:normal;opacity:0.6">v9.11</span></h1>
++  <h1>HiTeXeR <span style="font-size:0.675em;font-weight:normal;opacity:0.6">v9.12</span></h1>
+   <div class="menu-bar">
+     <div class="menu-item" id="menu-file">
+       <span>File</span>
diff --git a/auto-fix/rejected-edits/mql9t8haz7e8.patch b/auto-fix/rejected-edits/mql9t8haz7e8.patch
new file mode 100644
index 00000000..b898831b
--- /dev/null
+++ b/auto-fix/rejected-edits/mql9t8haz7e8.patch
@@ -0,0 +1,213 @@
+diff --git a/asy-interp.js b/asy-interp.js
+index 515e6bc1..9ca020b1 100644
+--- a/asy-interp.js
++++ b/asy-interp.js
+@@ -26917,6 +26917,25 @@ function renderSVG(result, opts) {
+             }
+           } catch (e) { /* fall back to heuristic */ }
+         }
++        // Multi-line (minipage) labels: the char-count × fontSize × 0.52 floors
++        // below over-reserve the flush-side width (the wrapped prose renders via
++        // real KaTeX glyph paths, ~30% narrower), padding the viewBox with phantom
++        // whitespace. Measure the widest actually-rendered line with the glyph
++        // engine — the SAME value the multi-line render path uses for its block
++        // width — so the bbox hugs the ink. (04702: minipage est 129bp → ~91bp.)
++        // Apply ONLY as a REDUCTION below the char-count render floor: a bogus
++        // OVER-measurement (e.g. 12382's "\vspace{-0.2cm}" tokens rendered as
++        // literal glyphs by the fallback) must not inflate the box and force the
++        // size() solver to collapse the geometry.
++        if (!_mjxMeasured && opts && opts.labelOutput === 'svg-native'
++            && typeof text === 'string' && text.indexOf('\n') !== -1
++            && typeof _measureMultilineMaxBp === 'function') {
++          try {
++            const _mlFloor = _effLenBB * fontSize * 0.52;
++            const _mlw = _measureMultilineMaxBp(text, fontSize);
++            if (_mlw > _mlFloor * 0.4 && _mlw < _mlFloor) { textWidthBpBase = _mlw; _mjxMeasured = true; }
++          } catch (e) { /* keep heuristic */ }
++        }
+         // Guard against severe width under-estimation for size()-constrained labels:
+         // the flat 0.288-em ratio is tight for alpha glyphs but too narrow for digits
+         // and mixed text+units labels (e.g. "0.650 kg"). Use the per-glyph estimator
+@@ -27066,7 +27085,10 @@ function renderSVG(result, opts) {
+             // the actual rendering's _effLen so we don't over-expand for labels
+             // like "e^{2πi/5}" whose visual width is smaller than char count.
+             let lWidthBp = textWidthBpBase;
+-            if (!hasFrac) {
++            // Skip the char-count render-width floor when textWidthBpBase is a
++            // real glyph measurement (frac/scaled/multi-line) — the 0.52em floor
++            // would re-inflate it (multi-line minipage: measured ~91bp → 129bp).
++            if (!hasFrac && !_mjxMeasured) {
+               const _renderWidthBp = _effLenBB * fontSize * 0.52;
+               if (_renderWidthBp > lWidthBp) lWidthBp = _renderWidthBp;
+             }
+@@ -29374,11 +29396,24 @@ function renderSVG(result, opts) {
+                 }
+               }
+             } else {
+-              // Multi-line: _mjxMeasureBp measures the concatenated single line
+-              // (MathJax ignores embedded \n), grossly over-wide, so keep the
+-              // char-count heuristic as the cap (guards shortstack/minipage
+-              // blowups like 03881, 04702).
+-              W = Math.min(W, measuredW * 1.05);
++              // Multi-line: the whole-string _mjxMeasureBp(dc.text) is grossly
++              // over-wide (MathJax ignores embedded \n), and the char-count W above
++              // over-reserves wrapped prose ~30% (real KaTeX glyphs are narrower) —
++              // padding the viewBox with phantom whitespace on the minipage's flush
++              // side (04702's 3.5cm caption made the canvas ~15% too wide). Measure
++              // each rendered line with the glyph engine and use the widest, capped
++              // by the heuristic so a measurement failure can't blow up the canvas.
++              if (typeof _measureMultilineMaxBp === 'function') {
++                const _mlFloor = _effLenVB * fontSizeSVG * 0.52;
++                const _mlw = _measureMultilineMaxBp(dc.text, fontSize) * bpCSSPixel;
++                // Use the per-line measurement only when it's a sane reduction
++                // (04702); a bogus over/under-measurement falls back to the
++                // concatenated-width cap so the viewBox can't blow up or clip (12382).
++                W = (_mlw > _mlFloor * 0.4 && _mlw < _mlFloor) ? Math.min(W, _mlw * 1.05)
++                                                               : Math.min(W, measuredW * 1.05);
++              } else {
++                W = Math.min(W, measuredW * 1.05);
++              }
+             }
+           }
+         } catch (e) { /* ignore — fall back to heuristic */ }
+@@ -30959,6 +30994,21 @@ function renderSVG(result, opts) {
+             }
+           } catch (e) { /* fall back to heuristic */ }
+         }
++        // Multi-line (minipage) block width: measure the widest rendered line with
++        // the glyph engine (the SAME value the bbox + viewBox-overshoot passes use),
++        // so a flushleft block centers on its real ink instead of the over-wide
++        // char-count heuristic — keeping the rendered block, the bbox, and the
++        // viewBox in agreement (04702: no phantom right margin, no left shift).
++        // Reduction-only (vs the char-count floor _labelWidthMeasured above) so a
++        // bogus over-measurement can't shove a flushleft block off-canvas (12382).
++        if (!_measured && opts && opts.labelOutput === 'svg-native'
++            && typeof _measureMultilineMaxBp === 'function' && _rawForMeas.indexOf('\n') !== -1) {
++          try {
++            const _mlFloor = _cleanLen * fontSizeSVG * 0.52;
++            const _mlw = _measureMultilineMaxBp(_rawForMeas, fontSizeSVG) * bpCSSPixel;
++            if (_mlw > _mlFloor * 0.4 && _mlw < _mlFloor) { _labelWidthMeasured = _mlw + fontSizeSVG * 0.1; _measured = true; }
++          } catch (e) { /* keep heuristic */ }
++        }
+         if (!_measured && typeof document !== 'undefined' && _rawForMeas.indexOf('\n') === -1) {
+           // Browser last resort: canvas ink metrics (emitter not ready yet).
+           try {
+@@ -31129,7 +31179,27 @@ function renderSVG(result, opts) {
+         // (plain_constants.asy), a raw-component test would silently drop
+         // EndPoint SE/NW axis labels out of this branch and shrink the
+         // figure bounds (00145 went from the ref's square aspect to 0.979).
+-        if (dc._isAxisLabel && Math.abs(axLinf) > 0.99 && !dc.labelTransform) {
++        // Extend the SVG-native text-anchor fast path to PURELY HORIZONTAL
++        // (E/W, ay≈0) NON-axis labels too. For these the renderer otherwise uses
++        // anchor='middle' with dx = 0.5·W + margin, where W is the measured width.
++        // The emitter then re-centers on its OWN glyph width Wₖ, so the left edge
++        // lands at anchor.x + margin + 0.5·(W − Wₖ): correct only when W == Wₖ.
++        // For a \footnotesize/\textbf caption the measurement double-applies the
++        // size selector (the folded fontSize already carries it, and KaTeX applies
++        // it again — see _katexMeasureBp), so W ≈ 0.8·Wₖ and the box shifts left
++        // by ~0.1·Wₖ, flush-clipping the leading glyph at the canvas edge (04702's
++        // bottom-left "E"). text-anchor='start'/'end' pins the W/E edge at the
++        // labelmargin offset regardless of any width-estimate error — identical to
++        // the well-measured case (W==Wₖ ⇒ same edge), so it is a no-op for accurate
++        // labels and a fix for mis-measured ones. Gate on ay≈0 so DIAGONAL aligns
++        // (NE/SE/…, where the 'middle' vertical-offset formula differs) are left
++        // untouched; only the axis-label branch keeps its diagonal coverage.
++        // Restrict the new non-axis case to SINGLE-LINE labels (numLines<=1): a
++        // multi-line minipage with W/E alignment (12382's "{\tiny1:1}…" blocks)
++        // lays its lines out via the tspan/block path below, where flipping the
++        // anchor shifts the whole block off its measured center.
++        if (Math.abs(axLinf) > 0.99 && !dc.labelTransform
++            && (dc._isAxisLabel || (Math.abs(ay) < 0.01 && numLines <= 1))) {
+           anchor = ax < 0 ? 'end' : 'start';
+           dx = axLinf * margin + axUnit * dotPush;
+           dy = -(ay_n * H + ayLinf * margin + ayUnit * dotPush);
+@@ -33623,6 +33693,45 @@ function _mjxMeasureBp(rawText, fontSize) {
+   return { wBp: parsed.wEx * exRatio * em * _MJX_SCALE, hBp: parsed.hEx * exRatio * em * _MJX_SCALE };
+ }
+ 
++// Widest actually-rendered line of a multi-line (minipage) label, in bp, using
++// the SAME glyph engine that draws it (katexSvg via _mjxMeasureBp). The
++// char-count × fontSize × 0.52 heuristic used elsewhere over-reserves wrapped
++// prose by ~30%; for a flushleft minipage that pads the viewBox with phantom
++// whitespace on the right (04702's 3.5cm caption made the canvas ~15% too wide).
++// `foldedFontSize` must ALREADY include any \scriptsize-class scale — the
++// selector is stripped here so _mjxMeasureBp doesn't fold it a second time.
++function _measureMultilineMaxBp(rawText, foldedFontSize) {
++  if (typeof _mjxMeasureBp !== 'function' || typeof rawText !== 'string') return 0;
++  const fsRe = /\\(?:tiny|scriptsize|footnotesize|small|normalsize|large|Large|LARGE|huge|Huge)(?![a-zA-Z])/g;
++  let maxW = 0;
++  for (let line of rawText.split('\n')) {
++    // Strip the \x06<c|l|r> minipage-align marker (line 0 only) and size selectors.
++    line = line.replace(/^\x06[clr]/, '').replace(fsRe, '');
++    // \( \) inline-math delimiters behave like $…$ (mirrors the render path).
++    line = line.replace(/\\\(/g, '$').replace(/\\\)/g, '$').trim();
++    if (!line) continue;
++    let measStr = line;
++    const hasMath = line.indexOf('$') !== -1 || /[\\^_]/.test(line);
++    if (!hasMath && /[A-Za-z0-9]/.test(line)) {
++      // Pure prose: measure as \text{…} (roman), not math — bare words measured
++      // as math come out ~25-35% too wide (italic + inter-letter math spacing).
++      let esc = '';
++      for (let i = 0; i < line.length; i++) {
++        const c = line[i];
++        if (c === '~') esc += ' ';
++        else if ('\\{}$&#^_%'.indexOf(c) !== -1) esc += '\\' + c;
++        else esc += c;
++      }
++      measStr = '\\text{' + esc + '}';
++    }
++    try {
++      const m = _mjxMeasureBp(measStr, foldedFontSize);
++      if (m && m.wBp > maxW) maxW = m.wBp;
++    } catch (e) { /* skip this line */ }
++  }
++  return maxW;
++}
++
+ // Browser-only counterpart to _mjxMeasureBp.  Renders the label through KaTeX
+ // into an off-screen DOM host at a probe font-size, reads the resulting
+ // getBoundingClientRect, and returns { wBp, hBp } scaled to the requested
+diff --git a/auto-fix/fix-snapshots/4779366a-after.png b/auto-fix/fix-snapshots/4779366a-after.png
+new file mode 100644
+index 00000000..ad0eff7c
+Binary files /dev/null and b/auto-fix/fix-snapshots/4779366a-after.png differ
+diff --git a/auto-fix/fix-snapshots/70efd1c4-after.png b/auto-fix/fix-snapshots/70efd1c4-after.png
+new file mode 100644
+index 00000000..c2cd60a0
+Binary files /dev/null and b/auto-fix/fix-snapshots/70efd1c4-after.png differ
+diff --git a/auto-fix/fix-snapshots/b62ccd58-after.png b/auto-fix/fix-snapshots/b62ccd58-after.png
+new file mode 100644
+index 00000000..ad0eff7c
+Binary files /dev/null and b/auto-fix/fix-snapshots/b62ccd58-after.png differ
+diff --git a/comparison/ssim-results.json b/comparison/ssim-results.json
+index 41634f12..651fc425 100644
+--- a/comparison/ssim-results.json
++++ b/comparison/ssim-results.json
+@@ -5341,11 +5341,11 @@
+     "id": "04702",
+     "idx": 4701,
+     "corpusFile": "c36_L2_script_0.asy",
+-    "ssim": 0.883248680695527,
++    "ssim": 0.9678431589722227,
+     "rawSsim": 0.4026560705152186,
+     "softSsim": 0.883248680695527,
+-    "sizeScore": 0.8240037097874373,
+-    "combined": 0.7278001895579739,
++    "sizeScore": 0.9500886338026268,
++    "combined": 0.9008346199551771,
+     "wRatio": 1.0933333333333333,
+     "hRatio": 1.0035087719298246,
+     "refDims": [
+diff --git a/index.html b/index.html
+index 51b088d9..9c0c589d 100644
+--- a/index.html
++++ b/index.html
+@@ -656,7 +656,7 @@ body {
+ <body>
+ 
+ <div id="toolbar">
+-  <h1>HiTeXeR <span style="font-size:0.675em;font-weight:normal;opacity:0.6">v9.13</span></h1>
++  <h1>HiTeXeR <span style="font-size:0.675em;font-weight:normal;opacity:0.6">v9.15</span></h1>
+   <div class="menu-bar">
+     <div class="menu-item" id="menu-file">
+       <span>File</span>
diff --git a/auto-fix/rejected-edits/mqlc2g17exak.patch b/auto-fix/rejected-edits/mqlc2g17exak.patch
new file mode 100644
index 00000000..e3ee92ff
--- /dev/null
+++ b/auto-fix/rejected-edits/mqlc2g17exak.patch
@@ -0,0 +1,224 @@
+diff --git a/asy-interp.js b/asy-interp.js
+index 515e6bc1..132b3d7d 100644
+--- a/asy-interp.js
++++ b/asy-interp.js
+@@ -25829,6 +25829,14 @@ function renderSVG(result, opts) {
+   // rule doesn't help one shape while regressing the other (verified 3-session
+   // residual). Use the raw result targets (pre bbox-margin / pre-solver mutation).
+   const _igWideAspect = (_keepAspect === false) && _sizeW > 0 && _sizeH > 0 && (_sizeW / _sizeH) >= 1.3;
++  // Pinning an endpoint-rotated y-title INTO the axis span (so it doesn't reserve
++  // ~half its rotated length as phantom top margin) applies to every NON-tall
++  // IgnoreAspect plot, not just wide ones: the render already pins it via
++  // _axisLabelEndShift regardless of aspect, so the bbox must mirror that or a
++  // square-ish plot like 08743 (AR 1.25, size(10cm,8cm)) leaves ~51bp empty above
++  // the box. Only genuinely TALL plots (06507 bar charts, AR<1) legitimately keep
++  // TeXeR's top space, so gate at AR >= 1.0 (a superset of _igWideAspect).
++  const _igNotTall = (_keepAspect === false) && _sizeW > 0 && _sizeH > 0 && (_sizeW / _sizeH) >= 1.0;
+   const dotfactor = _dotfactor || 6;
+   if (drawCommands.length === 0) return { svg:'<svg xmlns="http://www.w3.org/2000/svg"></svg>', commandMap: [], warnings: (result.directionWarnings || []).slice() };
+ 
+@@ -28034,8 +28042,22 @@ function renderSVG(result, opts) {
+         const cy = li.posY * pxPerUnitY + li.alignOffsetYBp;
+         bpMinX = Math.min(bpMinX, cx - wBp / 2);
+         bpMaxX = Math.max(bpMaxX, cx + wBp / 2);
+-        bpMinY = Math.min(bpMinY, cy - hBp / 2);
+-        bpMaxY = Math.max(bpMaxY, cy + hBp / 2);
++        // Mirror the heuristic solver's narrow-Y treatment for 90°-rotated
++        // horizontally-aligned axis titles (the AoPS y-axis-label idiom, e.g.
++        // 08743 "Sound Pressure (mPa)", 08812 "Percentage of True Speed").
++        // These run ALONGSIDE the axis; their full rotated height (= original
++        // text WIDTH, ~110 bp) is not a perpendicular Y-extent. Using it here
++        // (the measured-recheck pass omitted this) re-inflated totalH and made
++        // the solver shrink pxPerUnitY, leaving a phantom top margin.
++        const _is90RotR = Math.abs(Math.abs(li._ltAngle) - 90) < 1;
++        const _isHorizAlR = Math.abs(li._axAl) > 0.99 && Math.abs(li._ayAl) < 0.01;
++        if (_is90RotR && _isHorizAlR && isIgnoreAspect) {
++          bpMinY = Math.min(bpMinY, cy - wBp / 2);
++          bpMaxY = Math.max(bpMaxY, cy + wBp / 2);
++        } else {
++          bpMinY = Math.min(bpMinY, cy - hBp / 2);
++          bpMaxY = Math.max(bpMaxY, cy + hBp / 2);
++        }
+       }
+       if (anyMeasured) {
+         const totalW = bpMaxX - bpMinX;
+@@ -28122,8 +28144,17 @@ function renderSVG(result, opts) {
+                 const cy = li.posY * pxPerUnitY + li.alignOffsetYBp;
+                 bMinX = Math.min(bMinX, cx - li.widthBp / 2);
+                 bMaxX = Math.max(bMaxX, cx + li.widthBp / 2);
+-                bMinY = Math.min(bMinY, cy - li.heightBp / 2);
+-                bMaxY = Math.max(bMaxY, cy + li.heightBp / 2);
++                // Narrow-Y for a 90°-rotated horizontally-aligned y-axis title
++                // (runs alongside the axis — see recheck pass above).
++                const _is90RotI = Math.abs(Math.abs(li._ltAngle) - 90) < 1;
++                const _isHorizAlI = Math.abs(li._axAl) > 0.99 && Math.abs(li._ayAl) < 0.01;
++                if (_is90RotI && _isHorizAlI && isIgnoreAspect) {
++                  bMinY = Math.min(bMinY, cy - li.widthBp / 2);
++                  bMaxY = Math.max(bMaxY, cy + li.widthBp / 2);
++                } else {
++                  bMinY = Math.min(bMinY, cy - li.heightBp / 2);
++                  bMaxY = Math.max(bMaxY, cy + li.heightBp / 2);
++                }
+               }
+               const tW = bMaxX - bMinX, tH = bMaxY - bMinY;
+               const eW = tgtW < Infinity ? tW / tgtW : 0;
+@@ -28327,8 +28358,20 @@ function renderSVG(result, opts) {
+                   const cy = li.posY * pxPerUnitY + li.alignOffsetYBp;
+                   bMinX = Math.min(bMinX, cx - li.widthBp / 2);
+                   bMaxX = Math.max(bMaxX, cx + li.widthBp / 2);
+-                  bMinY = Math.min(bMinY, cy - li.heightBp / 2);
+-                  bMaxY = Math.max(bMaxY, cy + li.heightBp / 2);
++                  // Narrow-Y for a 90°-rotated horizontally-aligned y-axis title
++                  // (runs alongside the axis): without this, the grow-to-fill loop
++                  // counts the title's full rotated length as Y-extent and thinks
++                  // the box is already filled, so it never grows the squished
++                  // geometry (08743's phantom top margin).
++                  const _is90RotG = Math.abs(Math.abs(li._ltAngle) - 90) < 1;
++                  const _isHorizAlG = Math.abs(li._axAl) > 0.99 && Math.abs(li._ayAl) < 0.01;
++                  if (_is90RotG && _isHorizAlG && isIgnoreAspect) {
++                    bMinY = Math.min(bMinY, cy - li.widthBp / 2);
++                    bMaxY = Math.max(bMaxY, cy + li.widthBp / 2);
++                  } else {
++                    bMinY = Math.min(bMinY, cy - li.heightBp / 2);
++                    bMaxY = Math.max(bMaxY, cy + li.heightBp / 2);
++                  }
+                 }
+                 const tW = bMaxX - bMinX, tH = bMaxY - bMinY;
+                 const uW = (tgtW < Infinity && tW > 0) ? tgtW / tW : 1;
+@@ -28724,7 +28767,7 @@ function renderSVG(result, opts) {
+       // ~26900) so the canvas doesn't reserve ~half its rotated length as phantom
+       // margin past the axis end (08812's y-title → ~42bp phantom top). Tall plots
+       // (06507) legitimately keep that space in TeXeR, so leave them untouched.
+-      const _esh = _igWideAspect ? (li._axisLabelEndShift || 0) : 0;
++      const _esh = _igNotTall ? (li._axisLabelEndShift || 0) : 0;
+       const cy = li.posY + li.alignOffsetYBp / pxPerUnitY - _esh * hh;
+       minX = Math.min(minX, cx - hw);
+       maxX = Math.max(maxX, cx + hw);
+@@ -30866,7 +30909,19 @@ function renderSVG(result, opts) {
+           dc.text = dc.text.slice(2);
+         }
+       }
+-      // Skip empty labels
++      // Skip empty labels: an empty/whitespace-only label draws no glyphs and
++      // must NOT emit a <text> node. The AoPS "blank axes" template emits
++      // `label("", (xmax/2,ymax), N, fontsize(20))` for an absent title; the SVG
++      // emitter otherwise wrote an empty <text font-size="20">, and
++      // render-and-score's expandViewBox pads every <text> by fontsize*0.6, so
++      // that stray node reserved ~half a fontsize of phantom top margin in the
++      // rasterised PNG (08743's box pushed ~78px down). Test the RAW text (only $
++      // delimiters + whitespace removed), NOT stripLaTeX — stripLaTeX collapses
++      // command-only labels that DO render a glyph (e.g. "\textdollar" → $, 09485)
++      // to empty, which would wrongly drop them.
++      if (typeof dc.text === 'string' && dc.text.replace(/\$/g, '').trim() === '') {
++        continue;
++      }
+       const sx = (dc.pos.x - minX) * pxPerUnitX;
+       const sy = (maxY - dc.pos.y) * pxPerUnitY;
+       // TeX caps very large requested fontsizes (font substitution at ~25pt)
+diff --git a/auto-fix/fix-snapshots/404a0ccf-after.png b/auto-fix/fix-snapshots/404a0ccf-after.png
+new file mode 100644
+index 00000000..fa4b38da
+Binary files /dev/null and b/auto-fix/fix-snapshots/404a0ccf-after.png differ
+diff --git a/auto-fix/fix-snapshots/66c7b6e7-after.png b/auto-fix/fix-snapshots/66c7b6e7-after.png
+new file mode 100644
+index 00000000..fa4b38da
+Binary files /dev/null and b/auto-fix/fix-snapshots/66c7b6e7-after.png differ
+diff --git a/auto-fix/fix-snapshots/b5e3ec8b-after.png b/auto-fix/fix-snapshots/b5e3ec8b-after.png
+new file mode 100644
+index 00000000..fa4b38da
+Binary files /dev/null and b/auto-fix/fix-snapshots/b5e3ec8b-after.png differ
+diff --git a/comparison/ssim-results.json b/comparison/ssim-results.json
+index 41634f12..19d27ede 100644
+--- a/comparison/ssim-results.json
++++ b/comparison/ssim-results.json
+@@ -2437,26 +2437,6 @@
+       506
+     ]
+   },
+-  {
+-    "id": "04452",
+-    "idx": 4451,
+-    "corpusFile": "c321_L100_script_5.asy",
+-    "ssim": 0.9809804299384477,
+-    "rawSsim": 0.30280725812322695,
+-    "softSsim": 0.8825902673961766,
+-    "sizeScore": 0.9994004908660793,
+-    "combined": 0.9803923232105022,
+-    "wRatio": 0.613013698630137,
+-    "hRatio": 0.7584415584415585,
+-    "refDims": [
+-      584,
+-      770
+-    ],
+-    "htxDims": [
+-      358,
+-      584
+-    ]
+-  },
+   {
+     "id": "12881",
+     "idx": 12880,
+@@ -11701,9 +11681,9 @@
+     "id": "08743",
+     "idx": 8742,
+     "corpusFile": "c539_L10_script_12.asy",
+-    "ssim": 0.919685296885913,
++    "ssim": 0.98255756572466,
+     "rawSsim": 0.3613360194457885,
+-    "softSsim": 0.919685296885913,
++    "softSsim": 0.98255756572466,
+     "sizeScore": 0.9917098922364517,
+     "combined": 0.9120610066661778,
+     "wRatio": 1.0193548387096774,
+@@ -15201,9 +15181,9 @@
+     "id": "08744",
+     "idx": 8743,
+     "corpusFile": "c539_L10_script_13.asy",
+-    "ssim": 0.9351873447207345,
++    "ssim": 0.9845254153466471,
+     "rawSsim": 0.20424408146246087,
+-    "softSsim": 0.9351873447207345,
++    "softSsim": 0.9845254153466471,
+     "sizeScore": 0.9997215866101146,
+     "combined": 0.9349269760419129,
+     "wRatio": 1.0035398230088495,
+@@ -63497,6 +63477,26 @@
+       220
+     ]
+   },
++  {
++    "id": "04452",
++    "idx": 4451,
++    "corpusFile": "c321_L100_script_5.asy",
++    "ssim": 0.9809804299384477,
++    "rawSsim": 0.30280725812322695,
++    "softSsim": 0.8825902673961766,
++    "sizeScore": 0.9994004908660793,
++    "combined": 0.9803923232105022,
++    "wRatio": 0.613013698630137,
++    "hRatio": 0.7584415584415585,
++    "refDims": [
++      584,
++      770
++    ],
++    "htxDims": [
++      358,
++      584
++    ]
++  },
+   {
+     "id": "01582",
+     "idx": 1581,
+diff --git a/index.html b/index.html
+index 51b088d9..916a862c 100644
+--- a/index.html
++++ b/index.html
+@@ -656,7 +656,7 @@ body {
+ <body>
+ 
+ <div id="toolbar">
+-  <h1>HiTeXeR <span style="font-size:0.675em;font-weight:normal;opacity:0.6">v9.13</span></h1>
++  <h1>HiTeXeR <span style="font-size:0.675em;font-weight:normal;opacity:0.6">v9.14</span></h1>
+   <div class="menu-bar">
+     <div class="menu-item" id="menu-file">
+       <span>File</span>
diff --git a/comparison/ssim-results.json b/comparison/ssim-results.json
index 41634f12..067da3df 100644
--- a/comparison/ssim-results.json
+++ b/comparison/ssim-results.json
@@ -1717,26 +1717,6 @@
       656
     ]
   },
-  {
-    "id": "04943",
-    "idx": 4942,
-    "corpusFile": "c398_L15_script_2.asy",
-    "ssim": 0.3910317568268993,
-    "rawSsim": 0.23460327652646604,
-    "softSsim": 0.3910317568268993,
-    "sizeScore": 0.23696257156215028,
-    "combined": 0.09265989066016747,
-    "wRatio": 1.2545454545454546,
-    "hRatio": 0.9818181818181818,
-    "refDims": [
-      770,
-      110
-    ],
-    "htxDims": [
-      966,
-      108
-    ]
-  },
   {
     "id": "12778",
     "idx": 12777,
@@ -1821,7 +1801,7 @@
     "id": "04944",
     "idx": 4943,
     "corpusFile": "c398_L15_script_3.asy",
-    "ssim": 0.5937267571811355,
+    "ssim": 0.9716859334215067,
     "rawSsim": 0.3577401791073548,
     "softSsim": 0.5937267571811355,
     "sizeScore": 0.23696257156215028,
@@ -1961,7 +1941,7 @@
     "id": "04946",
     "idx": 4945,
     "corpusFile": "c398_L15_script_5.asy",
-    "ssim": 0.6689320820980403,
+    "ssim": 0.9795173401956118,
     "rawSsim": 0.3486473011989841,
     "softSsim": 0.6689320820980403,
     "sizeScore": 0.23696257156215028,
@@ -2041,7 +2021,7 @@
     "id": "04945",
     "idx": 4944,
     "corpusFile": "c398_L15_script_4.asy",
-    "ssim": 0.6769941942909169,
+    "ssim": 0.9796203409098745,
     "rawSsim": 0.37195570229873975,
     "softSsim": 0.6769941942909169,
     "sizeScore": 0.23696257156215028,
@@ -2141,7 +2121,7 @@
     "id": "04947",
     "idx": 4946,
     "corpusFile": "c398_L15_script_6.asy",
-    "ssim": 0.6328179406693129,
+    "ssim": 0.9823315501158361,
     "rawSsim": 0.3293663550750088,
     "softSsim": 0.6328179406693129,
     "sizeScore": 0.26852041299917323,
@@ -2437,26 +2417,6 @@
       506
     ]
   },
-  {
-    "id": "04452",
-    "idx": 4451,
-    "corpusFile": "c321_L100_script_5.asy",
-    "ssim": 0.9809804299384477,
-    "rawSsim": 0.30280725812322695,
-    "softSsim": 0.8825902673961766,
-    "sizeScore": 0.9994004908660793,
-    "combined": 0.9803923232105022,
-    "wRatio": 0.613013698630137,
-    "hRatio": 0.7584415584415585,
-    "refDims": [
-      584,
-      770
-    ],
-    "htxDims": [
-      358,
-      584
-    ]
-  },
   {
     "id": "12881",
     "idx": 12880,
@@ -3177,6 +3137,26 @@
       830
     ]
   },
+  {
+    "id": "04943",
+    "idx": 4942,
+    "corpusFile": "c398_L15_script_2.asy",
+    "ssim": 0.9391528958168714,
+    "rawSsim": 0.23460327652646604,
+    "softSsim": 0.3910317568268993,
+    "sizeScore": 0.9623470925457027,
+    "combined": 0.47504542482424167,
+    "wRatio": 1.2545454545454546,
+    "hRatio": 0.9818181818181818,
+    "refDims": [
+      770,
+      110
+    ],
+    "htxDims": [
+      966,
+      108
+    ]
+  },
   {
     "id": "12761",
     "idx": 12760,
@@ -63497,6 +63477,26 @@
       220
     ]
   },
+  {
+    "id": "04452",
+    "idx": 4451,
+    "corpusFile": "c321_L100_script_5.asy",
+    "ssim": 0.9809804299384477,
+    "rawSsim": 0.30280725812322695,
+    "softSsim": 0.8825902673961766,
+    "sizeScore": 0.9994004908660793,
+    "combined": 0.9803923232105022,
+    "wRatio": 0.613013698630137,
+    "hRatio": 0.7584415584415585,
+    "refDims": [
+      584,
+      770
+    ],
+    "htxDims": [
+      358,
+      584
+    ]
+  },
   {
     "id": "01582",
     "idx": 1581,
diff --git a/index.html b/index.html
index 51b088d9..916a862c 100644
--- a/index.html
+++ b/index.html
@@ -656,7 +656,7 @@ body {
 <body>
 
 <div id="toolbar">
-  <h1>HiTeXeR <span style="font-size:0.675em;font-weight:normal;opacity:0.6">v9.13</span></h1>
+  <h1>HiTeXeR <span style="font-size:0.675em;font-weight:normal;opacity:0.6">v9.14</span></h1>
   <div class="menu-bar">
     <div class="menu-item" id="menu-file">
       <span>File</span>
diff --git a/katex-svg.js b/katex-svg.js
index 89f68bb5..93f1213f 100644
--- a/katex-svg.js
+++ b/katex-svg.js
@@ -315,7 +315,17 @@
       // vlists (fractions, sub/sup stacks) left-align.
       const center = !!ctx.centerCols;
       for (const r of rows) {
-        const shiftUpEm = -(r.topEm + r.pstrut) - r.contentDepth;
+        // KaTeX places each vlist row so its content BASELINE sits at
+        // (top + pstrutHeight) below the vlist top reference (= ctx.y here);
+        // this is independent of the content's own depth. The earlier
+        // `- r.contentDepth` term was a latent bug — harmless for every common
+        // vlist (fraction num/den, sup/sub, sqrt radicand all report row-content
+        // depth 0) but it pushed any depth-bearing row DOWN by its own depth.
+        // That mis-placed the nested munder inside \underbrace{...}_{n}: the
+        // brace (depth ~0.65em) dropped a full 0.65em, opening a big gap under
+        // the braced content and crashing the brace into the subscript label
+        // (04943-04947). Use the exact KaTeX baseline.
+        const shiftUpEm = -(r.topEm + r.pstrut);
         const rowBaseY = ctx.y - shiftUpEm * ctx.s;
         const content = r.content;
         const cstyle = content.style || {};
